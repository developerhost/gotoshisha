# Prisma
DATABASE_URL="file:./dev.db"

# Cloudflare D1 (本番環境では自動的に設定される)
# DATABASE_URL は wrangler dev 時に自動的に設定されます

# 開発環境用の設定
ENVIRONMENT=development

# その他の環境変数（必要に応じて追加）
# JWT_SECRET=your-jwt-secret
# CORS_ORIGIN=http://localhost:3000
