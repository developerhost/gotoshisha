{
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    "strict": true,
    "types": ["vitest/globals"]
  },
  "exclude": [
    "backend/**/*",
    "node_modules",
    ".expo",
    "dist",
    "build"
  ]
}
