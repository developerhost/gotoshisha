{
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    "strict": true,
    "types": ["vitest/globals"]
  },
  "include": ["**/*.ts", "**/*.tsx", "vitest.d.ts"],
  "exclude": ["backend/**/*", "node_modules", ".expo", "dist", "build"]
}
